{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["apps/frontend/.next/**", "apps/frontend/dist/**", "apps/backend/target/**"]
    },
    "lint": {}
  }
}
